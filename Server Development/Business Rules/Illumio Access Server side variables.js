gs.include('IllumioConstants');
(function executeRule(current, previous /*null when async*/ ) {

    g_scratchpad.HTTP_RETRY_COUNT_MAX = HTTP_RETRY_COUNT_MAX;
    g_scratchpad.HTTP_RETRY_INTERVAL_INCREMENT_MAX = HTTP_RETRY_INTERVAL_INCREMENT_MAX;
    g_scratchpad.HTTP_RETRY_INTERVAL_MAX = HTTP_RETRY_INTERVAL_MAX;

})(current, previous);